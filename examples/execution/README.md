# S7 execution examples

Execution examples are generated from the safe planning fixtures and run only in `mock`, `dry_run`, or `from_artifacts` mode. They contain no real robot command, executable path, argv, hardware authorization, or dangerous physical pose. Use `scripts/run_compiled_task_graph.py` with a compiled graph from the S6 compiler.
