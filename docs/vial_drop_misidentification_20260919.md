# 第二支 vial 落料点误识别

现场记录：`/tmp/agentic_skills_runs/demo_4_vial_extract_20260919_132912_827330`。
第二支已通过拔出步骤，随后 `drop_area_2` 识别了红架本身，未发送 `drop_2_right`。
图像 `drop_area_2_panel.jpg` 绿色框位于红架上，不能将该结果解释为允许距离太严格。

红架参考 base Y 为 -0.077151 m；第一次合格落料点 Y 为 -0.202390 m，
右移 0.125239 m。第二次错误点 Y 为 -0.061227 m，相当于向左 0.015924 m。
参考红架像素框 [353,148,452,261]；错误落料框 [371.2,168,403.2,216] 在框内。
第一轮 VLM 回复完整，但标注错误，不是 JSON 截断或 P2P 容差问题。

修复：

- 提示加入固定头部相机红架参考框、允许右侧像素范围，并排除红架和已落料 vial。
- 图像检查要求落料框 x_min >= 红架 x_max + 5 px；确认参考/当前图像尺寸一致。
- 保留已有三维右侧/前后/高度检查，不截断或平移错误坐标来制造合格结果。
- 不合格候选默认最多重新采图识别一次，加入拒绝原因；图像、JSON、配置分别保存。
- 仅通过检查才调用落料 helper；运动失败不触发本重试。显式 drop_xyz 继续使用原坐标检查。
- 终端显示实际右移量和前后偏移，避免所有坐标失败都只显示范围。

37 项离线编排测试通过，包括这次错误框/第一次正确框回放、错误后重识别通过、
连续错误不发送落料、图像尺寸变化拒绝及默认四支流程。未执行在线识别或机械臂动作。
参考框依赖固定的红架和头部相机，识别框合格仍不等于对桌面障碍物的完整验证。
